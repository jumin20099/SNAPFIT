# 토큰 자동 갱신 테스트 시나리오

## 🎯 사용자 시나리오 테스트

### 시나리오 1: 일반 사용자
1. **로그인**
   - 브라우저에서 http://localhost:3000 접속
   - 카카오 로그인 또는 테스트 토큰 생성
   - 로그인 성공 확인

2. **일반적인 사용**
   - 커뮤니티 페이지에서 게시글 조회
   - 게시글 작성/수정/삭제
   - 프로필 페이지 접근
   - 30분 동안 계속 사용

3. **토큰 갱신 확인**
   - 개발자 도구 > Network 탭에서 API 요청 확인
   - 30분 후 자동 갱신 로그 확인
   - 토큰 갱신 토스트 메시지 확인

### 시나리오 2: 장시간 사용자
1. **초기 로그인**
   - 로그인 후 브라우저 탭을 열어둠
   - 다른 작업을 하다가 30분 후 돌아옴

2. **자동 갱신 확인**
   - 페이지 새로고침 없이 API 호출
   - 자동 토큰 갱신 과정 확인
   - 사용자 경험 끊김 없음 확인

3. **Refresh Token 만료 테스트**
   - 7일 후 다시 접속
   - 세션 만료 모달 확인
   - 로그인 페이지 리다이렉트 확인

### 시나리오 3: 네트워크 불안정 상황
1. **네트워크 끊김 시뮬레이션**
   - 개발자 도구 > Network 탭 > Offline 체크
   - API 호출 시도
   - 네트워크 복구 후 자동 재시도 확인

2. **서버 오류 시뮬레이션**
   - 백엔드 서버 중지
   - API 호출 시도
   - 서버 재시작 후 자동 재시도 확인

## 🔧 개발자 도구 활용

### Network 탭에서 확인할 것들
1. **토큰 갱신 요청**
   - `/api/auth/refresh` 요청 확인
   - 요청/응답 헤더 확인
   - 쿠키 전송 확인

2. **재시도 로직**
   - 401 응답 후 자동 재시도 확인
   - 원래 요청이 올바르게 재전송되는지 확인

3. **에러 처리**
   - 토큰 갱신 실패 시 로그아웃 확인
   - 적절한 에러 메시지 표시 확인

### Console 탭에서 확인할 것들
1. **토큰 갱신 로그**
   ```
   [TokenManager] 토큰 갱신 시작
   [TokenManager] 토큰 갱신 성공
   [ApiClient] 요청 재시도 성공
   ```

2. **에러 로그**
   ```
   [TokenManager] 토큰 갱신 실패: 세션이 만료되었습니다
   [ApiClient] 로그아웃 처리
   ```

### Application 탭에서 확인할 것들
1. **쿠키 저장**
   - `access_token` 쿠키 확인
   - `refresh_token` 쿠키 확인
   - httpOnly 설정 확인

2. **LocalStorage**
   - `accessToken` 저장 확인
   - `refreshToken` 저장 확인

## 🎭 실제 사용자 행동 패턴

### 패턴 1: 빠른 사용자
- 로그인 → 게시글 조회 → 게시글 작성 → 로그아웃
- 시간: 5-10분
- 토큰 갱신 없이 정상 동작 확인

### 패턴 2: 일반 사용자
- 로그인 → 다양한 페이지 탐색 → 30분 후 활동
- 시간: 30분 이상
- 자동 토큰 갱신 확인

### 패턴 3: 장시간 사용자
- 로그인 → 하루 종일 사용 → 다음날 접속
- 시간: 24시간 이상
- Refresh Token 갱신 확인

### 패턴 4: 비활성 사용자
- 로그인 → 7일간 비활성 → 재접속
- 시간: 7일 후
- 세션 만료 및 재로그인 확인

## 🚨 에러 상황 테스트

### 상황 1: 잘못된 토큰
```javascript
// 브라우저 콘솔에서 실행
localStorage.setItem('accessToken', 'invalid_token');
// API 호출 시 자동 갱신 시도 확인
```

### 상황 2: 만료된 Refresh Token
```javascript
// 브라우저 콘솔에서 실행
localStorage.setItem('refreshToken', 'expired_refresh_token');
// API 호출 시 로그아웃 처리 확인
```

### 상황 3: 네트워크 오류
- 개발자 도구 > Network 탭 > Offline 체크
- API 호출 시도
- 네트워크 복구 후 자동 재시도 확인

## 📱 모바일 테스트

### 모바일 브라우저에서 테스트
1. **Chrome DevTools 모바일 시뮬레이션**
   - F12 > Toggle device toolbar
   - 모바일 화면으로 전환
   - 터치 인터랙션 테스트

2. **실제 모바일 기기**
   - 같은 네트워크에서 모바일 브라우저로 접속
   - 실제 사용자 경험 확인

## 🔍 성능 테스트

### 토큰 갱신 성능
1. **갱신 시간 측정**
   - 개발자 도구 > Performance 탭
   - 토큰 갱신 과정 시간 측정

2. **메모리 사용량**
   - 개발자 도구 > Memory 탭
   - 토큰 관리 메모리 사용량 확인

3. **네트워크 사용량**
   - 개발자 도구 > Network 탭
   - 토큰 갱신 요청 크기 확인
